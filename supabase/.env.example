# Secrets da Edge Function connect-token.
# Copie para supabase/.env, preencha com suas credenciais reais e rode:
#   supabase secrets set --env-file supabase/.env
# supabase/.env está no .gitignore — NUNCA commitar.
PLUGGY_CLIENT_ID=seu_client_id_aqui
PLUGGY_CLIENT_SECRET=seu_client_secret_aqui
